@model IEnumerable<TestData>
    @{
        ViewData["Title"] = "Azure Sql Server";
    }
    <div class="jumbotron">
        <h2>Test data queried from Azure Sql Server:</h2>
    </div>
    <table class="table">
        <thead>
            <tr>
                <td>Id</td>
                <td>Data</td>
            </tr>
        </thead>
        <tbody>
            @foreach (var td in Model)
            {
                <tr>
                    <td>@td.Id</td>
                    <td>@td.Data</td>
                </tr>
            }
        </tbody>
    </table>
